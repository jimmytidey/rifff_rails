<% if @sound_file.new_record? %>
  alert("Failed to upload model: <%= j @@sound_file.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $('#file_list').append("<%= escape_javascript(render(@sound_file)) %>");
  $('#file_list').append("<%= escape_javascript(render(@sound_file)) %>");
<% end %>
rifff.loadSoundsLocations();